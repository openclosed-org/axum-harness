# SOPS + age — Secret Management

See [SETUP.md](./SETUP.md) for SOPS setup, configuration, age key management, host env-file export, and Flux integration. The current repository command surface is `just sops-*`; do not introduce committed `.env` files for backend deployables.
