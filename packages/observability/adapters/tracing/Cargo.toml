[package]
name = "adapter-telemetry-tracing"
version = "0.1.0"
edition = "2024"
publish = false
description = "tracing-subscriber integration for structured logging"

[dependencies]
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
tracing-error = { workspace = true }
workspace-hack = { version = "0.1", path = "../../../workspace-hack" }
